    #{attr_name}:
      validations: {}
      remoteName: "#{remote_name}"  # optional if the same as attribute's name
      type: "#{attr_type}"  # converts attribute's value to specified type (optional)